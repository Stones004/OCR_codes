# ==========================================================
# ocr_dataset_builder environment
#
# Covers BOTH OCR backends (src/config.py: OCR_BACKEND) -- one venv can
# run either "mistral" or "parseq", toggled with --backend / OCR_BACKEND.
#
#   python -m venv venv
#   venv\Scripts\pip install -r requirements.txt
#
# GPU note: torch/torchvision below are the CUDA 12.8 build matching the
# driver already installed on this machine. If you're on a machine without
# a compatible NVIDIA GPU, drop the "+cu128" suffixes (and the
# --extra-index-url line) to get the CPU build instead -- the "parseq"
# backend falls back to CPU automatically, just slower.
# ==========================================================

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# --- Local PARSeq backend ---
pytorch-lightning==2.6.5
timm==1.0.29
nltk==3.10.3
PyYAML==6.0.3
numpy==1.26.4
-e git+https://github.com/baudm/parseq.git@1902db043c029a7e03a3818c616c06600af574be#egg=strhub

# --- Mistral API backend + shared orchestration ---
requests==2.34.2
python-dotenv==1.2.2
tqdm==4.68.3
pillow==12.3.0
